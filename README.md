# Welcome Wyzetalk Node/Express App

## Available Scripts

In the project directory, you can run to install the dependencies:

### `npm install`

To run the app on http://localhost:5000

### `npm start`
